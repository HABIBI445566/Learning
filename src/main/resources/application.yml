server:
  port: 8082

spring:
  config:
    activate:
      on-profile: "prod"
  application:
    name: Spring-boot-learning
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: HabibZaid@123
    url: jdbc:mysql://localhost:3306/dcbapp-prod
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
welcome:
  message: Hey this is from properties file

---

spring:
  config:
    activate:
      on-profile: "dev"
  application:
    name: Spring-boot-learning
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: HabibZaid@123
    url: jdbc:mysql://localhost:3306/dcbapp
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
welcome:
  message: Hey this is from properties file

---

spring:
  config:
    activate:
      on-profile: "qa"
  application:
    name: Spring-boot-learning
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: HabibZaid@123
    url: jdbc:mysql://localhost:3306/dcbapp-qa
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
welcome:
  message: Hey this is from properties file
